import { division } from "./division";

test("Test", () => {
    expect(division(6, 2)).toBe(3);
});
test("Test2", () => {
    expect(division(1, 0)).toBe("Division by zero");
});
test("Test2", () => {
    expect(division(1, 0)).toBe("Division by zero");
});